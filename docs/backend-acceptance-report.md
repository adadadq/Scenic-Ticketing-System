# 后端验收报告

日期：2026-07-03

范围：第一阶段游客购票 MVP 的 FastAPI 后端、第二阶段管理员认证、后台订单只读 API、单张票码核销 API、批量核销 API、撤销核销 API、批量撤销核销 API、撤销核销原因审计、核销审计日志、核销/撤销失败尝试审计、核销/撤销失败尝试审计 CSV 导出、核销审计日志检索、核销审计日志原因筛选、核销审计日志 CSV/XLSX 导出、全单模拟退款 API、部分模拟退款 API、退款审计日志、退款审计日志检索、退款审计日志 CSV/XLSX 导出、后台运营汇总报表 API、后台支付对账汇总 API、后台支付对账汇总 CSV/XLSX 导出、后台产品维度报表 API、后台产品维度报表 CSV/XLSX 导出、后台日报趋势报表 API、后台小时趋势报表 API、后台月度趋势报表 API、后台趋势报表补零、后台趋势报表 CSV/XLSX 导出、后台同步导出行数上限、后台异步导出任务基础、后台异步导出任务文件格式筛选、后台异步导出 filters 白名单、后台异步导出 worker 状态机基础、后台异步导出失败字段长度契约对齐、后台异步导出文件下载端点、后台异步导出失败任务手动/自动重试、后台异步导出自动重试延迟领取、后台异步导出 RUNNING 超时回收、后台异步导出 RUNNING 超时最终失败告警事件记录、后台异步导出本地孤儿文件补偿清理、后台异步导出失败告警 provider 边界、后台异步导出最终失败告警事件记录、后台异步导出告警事件只读查询 API、后台异步导出告警事件汇总 API、后台异步导出告警事件确认 API、后台异步导出告警事件批量确认 API、后台异步导出告警事件关闭和重开 API、后台异步导出告警事件批量关闭 API、后台异步导出告警事件类型和格式筛选、后台异步导出告警事件删除 API、后台异步导出告警事件批量删除 API、后台异步订单明细 CSV/XLSX 生成 worker、后台异步核销审计 CSV/XLSX 生成 worker、后台异步核销失败审计 CSV/XLSX 生成 worker、后台异步退款审计 CSV/XLSX 生成 worker、后台异步支付对账 CSV/XLSX 生成 worker、后台异步产品维度报表 CSV/XLSX 生成 worker、后台异步日报趋势 CSV/XLSX 生成 worker、后台异步小时趋势 CSV/XLSX 生成 worker、后台异步月度趋势 CSV/XLSX 生成 worker、模拟支付回调安全边界、后台订单 CSV 导出、后台订单 XLSX 导出、API 契约、后端安全边界、OpenAPI 导出，以及前端共享 API 层的最小互通门禁。不覆盖前端视觉响应式、浏览器 E2E、真实支付渠道、退款通知、真实渠道退款流水号、生产对象存储、真实告警、真实渠道结算级财务对账、生产级大文件传输和生产部署。

## 本轮验收命令

```bash
scripts/verify-integration.sh
```

本轮结果：

- 后端游客购票闭环：`backend/tests/test_visitor_flow_api.py`，2 passed。
- 后端全量测试：`backend/tests`，680 passed。
- OpenAPI 离线契约：`scripts/export-openapi.py` 导出成功，并通过 `paths` 与 `components.schemas` 结构检查。
- 前端共享 API 互通门禁：`npm run lint` 和 `npm run build` 通过。
- Vite 仍提示单个 chunk 超过 500 kB，这是前端构建体积提醒，不影响当前后端 API 验收。

后端切片提交前的默认验收入口仍是：

```bash
scripts/verify-backend.sh
```

需要浏览器 smoke 时显式运行：

```bash
VERIFY_INTEGRATION_E2E=1 scripts/verify-integration.sh
```

## 已验证能力

- 游客可完成 CSRF 初始化、手机号登录、实名注册、票品浏览、创建订单、模拟支付、查看订单和取消待支付订单。
- 除 CSV 文件下载外，所有已实现 JSON 接口保持统一成功壳 `success/data/request_id`；所有失败响应保持统一失败壳 `success/code/message/request_id`。
- OpenAPI 200 响应指向对应 DTO，错误响应统一为 `ApiFailureDTO`，并声明 `x-request-id` 响应头。
- 前端共享 API 的 method、path、query key、返回类型、CSRF Cookie 名、CSRF header 和支付 `Idempotency-Key` 均由后端测试对齐。
- 后端里程碑状态矩阵 `docs/backend-milestone-status.md` 覆盖阶段 0 到阶段 8，并明确第二阶段功能必须先补契约和威胁模型。
- 第二阶段管理员权限基座规格 `docs/superpowers/specs/2026-06-30-admin-auth-foundation-design.md` 已明确 API、DTO、权限、CSRF、登录限速、PBKDF2 口令哈希和验收边界。
- 管理员权限基座已实现：管理员登录、当前管理员、管理员退出、管理员 session、游客/管理员权限隔离、管理员登录限速和 PBKDF2 口令哈希均由 `backend/tests/test_admin_auth_api.py` 覆盖。
- 第二阶段禁用管理员既有 session 边界已实现：规格 `docs/superpowers/specs/2026-07-02-disabled-admin-session-boundary-design.md` 已明确账号登录后被禁用时既有 session 不能继续访问后台；实现由 `backend/tests/test_admin_auth_api.py::test_disabled_admin_existing_session_is_rejected_and_revoked` 覆盖统一 `ADMIN_AUTH_REQUIRED` 和服务端 session 撤销。
- 登录限流 provider 边界已补充：`LOGIN_RATE_LIMIT_PROVIDER` 当前只允许 `memory`，未知全局限流 provider 会在配置校验阶段拒绝启动；实现由 `backend/tests/test_config_db.py` 覆盖默认值、大小写归一化和未知 provider 拒绝。
- 第二阶段后台订单只读 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-order-read-model-design.md` 已明确 API、DTO、权限、筛选分页、错误码和验收边界；实现由 `backend/tests/test_admin_orders_api.py` 覆盖管理员订单列表、管理员订单详情、后台订单 DTO、管理员权限边界和手机号脱敏。
- 第二阶段后台票码核销 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-check-in-design.md` 已明确状态机、CSRF、权限、DTO 和错误码；实现由 `backend/tests/test_admin_check_in_api.py` 覆盖单张票码核销、重复核销、状态不可核销、管理员权限、session-bound CSRF 和库存核销计数。
- 第二阶段后台批量核销 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-batch-check-in-design.md` 已明确批量请求 DTO、逐票业务结果、管理员权限、CSRF、审计复用和验收边界；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖批量核销、逐票业务失败、请求体校验、管理员权限、session-bound CSRF、成功票码审计和 OpenAPI DTO。
- 第二阶段后台撤销核销 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-undo-check-in-design.md` 已明确状态机、CSRF、权限、DTO、错误码、库存核销量回退和审计动作；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_openapi_contract.py` 与 `backend/tests/test_schema_contract.py` 覆盖撤销核销、重复撤销防护、状态不可撤销、管理员权限、session-bound CSRF、DTO 防泄露、`COMPLETED -> PAID`、`quota_checked_in` 非负条件、`UNDO_CHECK_IN` 审计和 OpenAPI DTO。
- 第二阶段后台批量撤销核销 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-batch-undo-check-in-design.md` 已明确批量请求 DTO、逐票业务结果、管理员权限、CSRF、审计复用和验收边界；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖批量撤销核销、逐票业务失败、请求体校验、管理员权限、session-bound CSRF、成功票码 `UNDO_CHECK_IN` 审计、重复撤销防护、系统异常不伪装成逐票结果和 OpenAPI DTO。
- 第二阶段撤销核销原因审计已实现：规格 `docs/superpowers/specs/2026-07-01-admin-undo-check-in-reason-audit-design.md` 已明确单张/批量撤销可选 reason、成功撤销审计落库、核销审计 DTO/导出字段、迁移和验收边界；`database/migrations/2026-07-01-add-check-in-audit-log-reason.sql` 支持已有运行库幂等补列；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_schema_contract.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 reason trim、空值/超长/额外字段拒绝、成功撤销审计 reason、CSV/XLSX 导出 reason 公式注入防护、XML 1.0 非法控制字符清洗、schema 约束、迁移 SQL 和 SQL 参数绑定。
- 第二阶段核销审计日志已实现：规格 `docs/superpowers/specs/2026-06-30-admin-check-in-audit-log-design.md` 已明确审计表、事务内写入、管理员只读查询、DTO 防泄露和错误码；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_schema_contract.py` 与 `backend/tests/test_openapi_contract.py` 覆盖核销成功审计写入、失败不写审计、审计写入异常交给连接事务回滚、管理员权限、只读 GET 无 CSRF、操作人展示字段、request id、schema 外键和 OpenAPI DTO。
- 第二阶段核销失败尝试审计已实现：规格 `docs/superpowers/specs/2026-07-01-admin-check-in-failure-audit-log-design.md` 已明确业务失败码、独立失败审计表、管理员只读检索、DTO 防泄露和验收边界；`database/migrations/2026-07-01-add-check-in-failure-audit-log.sql` 支持已有运行库幂等补表；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_openapi_contract.py` 与 `backend/tests/test_schema_contract.py` 覆盖单张/批量核销业务失败写入、权限隔离、只读 GET 无 CSRF、非法失败码、非法日期范围、OpenAPI DTO、schema 约束、迁移 SQL 和 SQL 参数绑定。
- 第二阶段撤销核销失败尝试审计已实现：规格 `docs/superpowers/specs/2026-07-01-admin-undo-check-in-failure-audit-log-design.md` 已明确撤销业务失败码、复用失败审计表、管理员只读检索、DTO 防泄露和验收边界；`database/migrations/2026-07-01-extend-check-in-failure-audit-log-for-undo.sql` 支持已有运行库幂等扩展约束；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_openapi_contract.py` 与 `backend/tests/test_schema_contract.py` 覆盖单张/批量撤销业务失败写入、权限隔离、系统异常不审计、OpenAPI 枚举、schema 约束、迁移 SQL 和 SQL 参数绑定。
- 第二阶段核销/撤销失败尝试审计 CSV 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-check-in-failure-audit-log-export-design.md` 已明确文件响应、失败审计筛选复用、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、非法失败码、非法日期范围、敏感字段防泄露、公式注入防护、OpenAPI 文件响应和 SQL 参数绑定。
- 第二阶段核销/撤销失败尝试审计 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-check-in-failure-audit-log-xlsx-export-design.md` 已明确文件响应、失败审计筛选复用、管理员权限、敏感字段边界、公式注入防护、XML 1.0 非法控制字符清洗和无新增运行时依赖；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、非法失败码、非法日期范围、敏感字段防泄露、无公式节点、XML 1.0 非法控制字符清洗和 OpenAPI 文件响应。
- 第二阶段核销审计日志检索已实现：规格 `docs/superpowers/specs/2026-06-30-admin-check-in-audit-log-search-design.md` 已明确全局检索、筛选分页、管理员只读权限、DTO 防泄露和错误码；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖按票码、订单号、操作人和日期筛选、分页、只读 GET 无 CSRF、非法日期范围、OpenAPI 参数和 SQL 参数绑定。
- 第二阶段核销审计日志原因筛选已实现：规格 `docs/superpowers/specs/2026-07-02-admin-check-in-audit-reason-filter-design.md` 已明确 `reason` 可用于核销审计检索、同步 CSV/XLSX 导出和异步 `CHECK_IN_AUDIT` 导出 filters；实现由 `backend/tests/test_admin_check_in_api.py`、`backend/tests/test_admin_export_jobs_api.py` 和前端共享 API 类型覆盖 reason trim、模糊匹配、SQL 参数绑定、公开 filters 脱敏和 worker 完整 filters 传递。
- 第二阶段核销审计日志 CSV 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-check-in-audit-log-export-design.md` 已明确文件响应、审计筛选复用、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、非法日期范围、撤销原因筛选、敏感字段防泄露、公式注入防护、OpenAPI 文件响应和 SQL 参数绑定。
- 第二阶段核销审计日志 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-check-in-audit-log-xlsx-export-design.md` 已明确文件响应、审计筛选复用、管理员权限、敏感字段边界、公式注入防护、XML 1.0 非法控制字符清洗和无新增运行时依赖；实现由 `backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、非法日期范围、撤销原因筛选、敏感字段防泄露、无公式节点、XML 1.0 非法控制字符清洗和 OpenAPI 文件响应。
- 第二阶段后台全单模拟退款 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-full-refund-design.md` 已明确状态机、CSRF、权限、DTO、库存回补和错误码；实现由 `backend/tests/test_admin_refund_api.py` 覆盖整单退款、重复退款、不可退款状态、管理员权限、session-bound CSRF 和库存售出量回补。
- 第二阶段后台部分模拟退款 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-partial-refund-design.md` 已明确按票项退款、CSRF、权限、DTO、库存回补、`PARTIAL_REFUND` 状态和错误码；实现由 `backend/tests/test_admin_refund_api.py` 覆盖部分退款、票项错误、不可退款状态、管理员权限、session-bound CSRF、DTO 防泄露、SQL 参数绑定和失败前无副作用。
- 第二阶段退款写操作 SUPER_ADMIN 边界已实现：规格 `docs/superpowers/specs/2026-07-02-admin-refund-super-admin-boundary-design.md` 已明确整单退款和部分退款只允许 `SUPER_ADMIN` 执行，`OPERATOR` 返回 `ADMIN_FORBIDDEN` 且不产生订单、库存、支付或退款审计副作用。
- 第二阶段退款审计日志已实现：规格 `docs/superpowers/specs/2026-06-30-admin-refund-audit-log-design.md` 已明确审计日志表、事务内写入、管理员只读查询、DTO 防泄露和错误码；实现由 `backend/tests/test_admin_refund_api.py` 与 `backend/tests/test_schema_contract.py` 覆盖整单/部分退款审计写入、管理员权限、只读 GET 无 CSRF、操作人展示字段、request id、JSONB 票项号和 schema 外键顺序。
- 第二阶段退款审计日志检索已实现：规格 `docs/superpowers/specs/2026-06-30-admin-refund-audit-log-search-design.md` 已明确全局检索、筛选分页、管理员只读权限、DTO 防泄露和错误码；实现由 `backend/tests/test_admin_refund_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖按退款类型、订单号、操作人和日期筛选、分页、只读 GET 无 CSRF、OpenAPI 枚举和 SQL 参数绑定。
- 第二阶段退款审计日志 CSV 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-refund-audit-log-export-design.md` 已明确文件响应、审计筛选复用、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_refund_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、非法筛选、敏感字段防泄露、公式注入防护、OpenAPI 文件响应和 SQL 参数绑定。
- 第二阶段退款审计日志 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-refund-audit-log-xlsx-export-design.md` 已明确文件响应、审计筛选复用、管理员权限、敏感字段边界、公式注入防护、XML 1.0 非法控制字符清洗和无新增运行时依赖；实现由 `backend/tests/test_admin_refund_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、非法筛选、敏感字段防泄露、无公式节点、inline string、XML 1.0 非法控制字符清洗和 OpenAPI 文件响应。
- 第二阶段后台运营汇总报表 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-report-summary-design.md` 已明确日期过滤、管理员权限、DTO、统计口径和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 覆盖汇总读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露和 SQL 参数绑定。
- 第二阶段后台支付对账汇总 API 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-payment-reconciliation-report-design.md` 已明确订单净收款、已捕获支付、退款审计金额和差异口径、管理员权限、DTO、日期过滤和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖对账读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露、`SUCCESS/REFUNDED` 支付记录口径、OpenAPI 成功壳和 SQL 参数绑定。
- 第二阶段后台支付对账汇总 CSV 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-payment-reconciliation-csv-export-design.md` 已明确同一日期口径的支付对账 CSV 文件响应、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、公式注入防护和 OpenAPI 文件响应。
- 第二阶段后台支付对账汇总 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-payment-reconciliation-xlsx-export-design.md` 已明确同一日期口径的支付对账 XLSX 文件响应、管理员权限、敏感字段边界、无公式节点和 XML 1.0 非法控制字符清洗；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、无公式节点、inline string 和 OpenAPI 文件响应。
- 第二阶段后台产品维度报表 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-product-breakdown-report-design.md` 已明确产品分组口径、管理员权限、DTO、日期过滤和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 覆盖产品分组读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露、订单数去重、明细金额聚合和 SQL 参数绑定。
- 第二阶段后台产品维度报表 CSV 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-product-breakdown-csv-export-design.md` 已明确同一日期口径的产品/票种分组 CSV 文件响应、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、公式注入防护和 OpenAPI 文件响应。
- 第二阶段后台产品维度报表 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-product-breakdown-xlsx-export-design.md` 已明确同一日期口径的产品/票种分组 XLSX 文件响应、管理员权限、敏感字段边界、无公式节点和 XML 1.0 非法控制字符清洗；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、inline string、无公式节点、XML 1.0 非法控制字符清洗和 OpenAPI 文件响应。
- 第二阶段后台日报趋势报表 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-daily-trend-report-design.md` 已明确日维度趋势口径、管理员权限、DTO、日期过滤和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 覆盖日报趋势读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露、订单金额与明细票数分开聚合和 SQL 参数绑定。
- 第二阶段后台小时趋势报表 API 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-hourly-trend-report-design.md` 已明确小时维度趋势口径、管理员权限、DTO、日期过滤和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖小时趋势读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露、订单金额与明细票数分开聚合、SQL 参数绑定和 OpenAPI 成功壳。
- 第二阶段后台月度趋势报表 API 已实现：规格 `docs/superpowers/specs/2026-06-30-admin-monthly-trend-report-design.md` 已明确月维度趋势口径、管理员权限、DTO、日期过滤和验收边界；实现由 `backend/tests/test_admin_reports_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖月度趋势读取、日期范围错误、管理员权限、只读 GET 无 CSRF、DTO 防泄露、订单金额与明细票数分开聚合、SQL 参数绑定和 OpenAPI 成功壳。
- 第二阶段后台趋势报表补零已实现：规格 `docs/superpowers/specs/2026-07-01-admin-trend-zero-fill-design.md` 已明确 `includeEmpty` 参数、日期边界、补零上限、权限和 DTO 边界；实现由 `backend/tests/test_admin_reports_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖日报、小时、月度趋势补零、缺少日期边界、范围过大、默认行为不变和 OpenAPI 参数。
- 第二阶段后台趋势报表 CSV 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-trend-csv-export-design.md` 已明确日报、小时和月度趋势 CSV 文件响应、`includeEmpty` 复用、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖三个 CSV 下载、趋势补零、管理员权限、只读 GET 无 CSRF、错误码复用、敏感字段防泄露、公式注入防护和 OpenAPI 文件响应。
- 第二阶段后台趋势报表 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-07-01-admin-trend-xlsx-export-design.md` 已明确日报、小时和月度趋势 XLSX 文件响应、`includeEmpty` 复用、管理员权限、敏感字段边界、无公式节点和 XML 1.0 非法控制字符清洗；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖三个 XLSX 下载、趋势补零、管理员权限、只读 GET 无 CSRF、错误码复用、敏感字段防泄露、无公式节点和 OpenAPI 文件响应。
- 第二阶段后台同步导出行数上限已实现：规格 `docs/superpowers/specs/2026-07-01-admin-sync-export-row-limit-design.md` 已明确多行同步 CSV/XLSX 导出的 5000 行上限、`SYNC_EXPORT_ROW_LIMIT + 1` 探测查询、`413 ADMIN_EXPORT_TOO_LARGE` 统一失败响应和参数化 `LIMIT` 边界；实现由 `backend/tests/test_admin_report_export_api.py`、`backend/tests/test_admin_check_in_api.py` 与 `backend/tests/test_admin_refund_api.py` 覆盖报表、订单明细、核销审计、核销失败审计和退款审计导出超限，并直接断言订单、产品维度、趋势、核销审计、核销失败审计和退款审计导出查询使用参数化 `LIMIT`。
- 第二阶段后台异步导出任务基础已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-foundation-design.md` 已明确导出任务表、创建/列表/详情 API、管理员权限、创建任务 CSRF、DTO 防泄露和本切片不生成文件的边界；实现由 `backend/tests/test_admin_export_jobs_api.py`、`backend/tests/test_openapi_contract.py` 与 `backend/tests/test_schema_contract.py` 覆盖任务创建、查询、权限、CSRF、过滤条件大小限制、OpenAPI 枚举、schema、迁移和 SQL 参数绑定。
- 第二阶段后台异步导出任务文件格式筛选已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-file-format-filter-design.md` 已明确管理员可通过 `fileFormat=CSV/XLSX` 筛选导出任务列表；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖大小写归一化、非法文件格式、OpenAPI、DTO 防泄露和 SQL 参数绑定。
- 第二阶段后台异步导出 filters 白名单已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-filter-whitelist-design.md` 已明确各 `exportType` 允许的 `filters` 字段、日期和枚举归一化、非法 filters 错误码以及不生成文件的边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖白名单、文本 trim、日期格式、日期倒挂、枚举大写、布尔归一化、未知字段、过长文本和 `ADMIN_EXPORT_JOB_FILTERS_INVALID`。
- 第二阶段后台异步导出公开 filters 脱敏已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-filter-redaction-design.md` 已明确公开 API 响应隐藏 `ticketCode`、`orderNo` 和 `operatorUsername`，worker 内部继续使用完整 `filters`；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖创建、列表、详情、重试响应脱敏以及 worker filters 不受影响。
- 第二阶段后台异步导出 worker 脚本输出脱敏已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-worker-script-redaction-design.md` 已明确单次 worker 和循环 worker 的 stdout 公开任务 DTO 必须隐藏敏感 `filters`，实现由 `backend/tests/test_openapi_export_script.py` 覆盖 `job.filters` 与 `lastJob.filters` 脱敏。
- 第二阶段后台异步导出任务 requestId 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-request-id-design.md` 已明确导出任务保存创建请求 id 仅用于排障关联；实现由 `backend/tests/test_admin_export_jobs_api.py` 与 `backend/tests/test_schema_contract.py` 覆盖 DTO `requestId`、仓储写入、SQL 参数绑定和旧库迁移。
- 第二阶段后台异步导出 worker 状态机基础已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-worker-state-design.md` 已明确内部 worker 的 `PENDING -> RUNNING -> SUCCEEDED/FAILED` 流转、行锁领取、状态条件更新、worker 输入长度限制和 DTO 防泄露边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖最早任务领取、无任务返回、成功/失败只更新 `RUNNING`、`storageKey` 不出 DTO、`FOR UPDATE SKIP LOCKED` 和 SQL 参数绑定。
- 第二阶段后台异步导出失败字段长度契约对齐已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-error-field-lengths-design.md` 已明确 `admin_export_job` 和 `admin_export_job_alert_event` 的错误字段列宽对齐服务层限制；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_schema_contract.py` 覆盖 worker 输入长度、schema 列宽和旧库迁移。
- 第二阶段后台异步导出文件下载端点已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-download-design.md` 已明确 `GET /api/admin/export-jobs/{job_id}/download` 的管理员权限、只读 GET 无 CSRF、文件响应、未生成任务错误、文件缺失错误和本地存储路径边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖成功下载 CSV、匿名/游客拒绝、未生成任务拒绝、缺失文件返回 `ADMIN_EXPORT_FILE_NOT_FOUND`、路径穿越、`Content-Disposition`、OpenAPI 文件响应和 SQL 参数绑定。
- 第二阶段后台异步订单明细 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-order-csv-worker-design.md` 已明确单次 worker 处理 `ORDER_DETAIL + CSV`、复用同步订单 CSV 导出口径、受控本地存储、暂不支持格式失败落库和脚本入口；实现由 `backend/tests/test_admin_export_jobs_api.py` 与 `backend/tests/test_openapi_export_script.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、未支持类型失败落库、无 pending 空跑、脚本 JSON 输出契约和脚本启动异常脱敏。
- 第二阶段后台异步订单明细 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-order-xlsx-worker-design.md` 已明确单次 worker 处理 `ORDER_DETAIL + XLSX`、复用同步订单 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、CSV 路径不回归、未支持类型失败落库，并复用订单 XLSX 的 inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步核销审计 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-check-in-audit-csv-worker-design.md` 已明确单次 worker 处理 `CHECK_IN_AUDIT + CSV`、复用同步核销审计 CSV 导出口径、受控本地存储和暂不支持格式失败落库；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、核销审计 filters 传递（含撤销原因）、订单导出路径不回归和未支持类型失败落库。
- 第二阶段后台异步核销审计 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-check-in-audit-xlsx-worker-design.md` 已明确单次 worker 处理 `CHECK_IN_AUDIT + XLSX`、复用同步核销审计 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、核销审计 filters 传递（含撤销原因）、CSV 路径不回归和未支持类型失败落库，并复用同步核销审计 XLSX 的 inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步核销失败审计 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-check-in-failure-audit-csv-worker-design.md` 已明确单次 worker 处理 `CHECK_IN_FAILURE_AUDIT + CSV`、复用同步核销失败审计 CSV 导出口径、受控本地存储和暂不支持格式失败落库；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、核销失败审计 filters 传递、核销审计路径不回归、未支持类型失败落库，并复用同步核销失败审计 CSV 的安全字段、失败码枚举校验和公式注入防护。
- 第二阶段后台异步核销失败审计 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-check-in-failure-audit-xlsx-worker-design.md` 已明确单次 worker 处理 `CHECK_IN_FAILURE_AUDIT + XLSX`、复用同步核销失败审计 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、核销失败审计 filters 传递、CSV 路径不回归和未支持类型失败落库，并复用同步核销失败审计 XLSX 的安全字段、失败码枚举校验、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步退款审计 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-refund-audit-csv-worker-design.md` 已明确单次 worker 处理 `REFUND_AUDIT + CSV`、复用同步退款审计 CSV 导出口径、受控本地存储和暂不支持格式失败落库；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_export_script.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、退款审计 filters 传递、worker 脚本注入退款服务和非法 filters 失败落库，并复用同步退款审计 CSV 的安全字段、退款类型枚举校验和公式注入防护；XLSX 已单独补齐。
- 第二阶段后台异步退款审计 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-01-admin-export-job-refund-audit-xlsx-worker-design.md` 已明确单次 worker 处理 `REFUND_AUDIT + XLSX`、复用同步退款审计 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、退款审计 filters 传递、CSV 路径不回归和未支持类型失败落库，并复用同步退款审计 XLSX 的安全字段、退款类型枚举校验、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步支付对账 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-payment-reconciliation-csv-worker-design.md` 已明确单次 worker 处理 `PAYMENT_RECONCILIATION + CSV`、复用同步支付对账 CSV 单行汇总口径和受控本地存储；实现由 `backend/tests/test_admin_export_jobs_api.py` 与 `backend/tests/test_schema_contract.py` 覆盖创建任务白名单、导出类型约束迁移、生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露和日期 filters 传递。
- 第二阶段后台异步支付对账 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-payment-reconciliation-xlsx-worker-design.md` 已明确单次 worker 处理 `PAYMENT_RECONCILIATION + XLSX`、复用同步支付对账 XLSX 单行汇总口径和受控本地存储；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 传递和 CSV 路径不回归。
- 第二阶段后台异步产品维度报表 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-product-breakdown-csv-worker-design.md` 已明确单次 worker 处理 `PRODUCT_BREAKDOWN + CSV`、复用同步产品维度 CSV 导出口径、受控本地存储和安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露和日期 filters 传递，并复用同步产品维度 CSV 的聚合字段、日期校验、敏感字段边界和公式注入防护；XLSX 已由后续切片补齐。
- 第二阶段后台异步产品维度报表 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-product-breakdown-xlsx-worker-design.md` 已明确单次 worker 处理 `PRODUCT_BREAKDOWN + XLSX`、复用同步产品维度 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 传递和未支持趋势任务失败落库，并复用同步产品维度 XLSX 的聚合字段、日期校验、敏感字段边界、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步日报趋势 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-daily-trend-csv-worker-design.md` 已明确单次 worker 处理 `DAILY_TREND + CSV`、复用同步日报趋势 CSV 导出口径、受控本地存储和补零安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步日报趋势 CSV 的聚合字段、补零范围校验、日期校验、敏感字段边界和公式注入防护。
- 第二阶段后台异步日报趋势 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-daily-trend-xlsx-worker-design.md` 已明确单次 worker 处理 `DAILY_TREND + XLSX`、复用同步日报趋势 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步日报趋势 XLSX 的聚合字段、补零范围校验、日期校验、敏感字段边界、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步小时趋势 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-hourly-trend-csv-worker-design.md` 已明确单次 worker 处理 `HOURLY_TREND + CSV`、复用同步小时趋势 CSV 导出口径、受控本地存储和补零安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步小时趋势 CSV 的聚合字段、31 天补零范围校验、日期校验、敏感字段边界和公式注入防护。
- 第二阶段后台异步小时趋势 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-hourly-trend-xlsx-worker-design.md` 已明确单次 worker 处理 `HOURLY_TREND + XLSX`、复用同步小时趋势 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步小时趋势 XLSX 的聚合字段、31 天补零范围校验、日期校验、敏感字段边界、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步月度趋势 CSV 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-monthly-trend-csv-worker-design.md` 已明确单次 worker 处理 `MONTHLY_TREND + CSV`、复用同步月度趋势 CSV 导出口径、受控本地存储和补零安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 CSV 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步月度趋势 CSV 的聚合字段、60 个月补零范围校验、日期校验、敏感字段边界和公式注入防护。
- 第二阶段后台异步月度趋势 XLSX 生成 worker 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-monthly-trend-xlsx-worker-design.md` 已明确单次 worker 处理 `MONTHLY_TREND + XLSX`、复用同步月度趋势 XLSX 导出口径、受控本地存储和表格安全边界；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖生成 XLSX 并标记 `SUCCEEDED`、后端派生 `storage_key`、DTO 防泄露、日期 filters 和 `includeEmpty` filters 传递，并复用同步月度趋势 XLSX 的聚合字段、60 个月补零范围校验、日期校验、敏感字段边界、inline string、无公式节点和 XML 1.0 非法控制字符清洗。
- 第二阶段后台异步导出 worker 循环入口已实现：`scripts/run-admin-export-worker.py` 可循环处理 pending 任务，实现由 `backend/tests/test_openapi_export_script.py` 覆盖 worker 服务构造、最大处理数、空闲退出、空闲 sleep、脚本 JSON 输出、参数错误脱敏和启动异常脱敏。
- 第二阶段后台异步导出 worker 进程守护模板已补充：`deploy/systemd/scenic-ticket-admin-export-worker.service` 可作为 Linux 部署的 systemd 参考，实现由 `backend/tests/test_openapi_export_script.py` 覆盖非 root 用户、环境文件、受控导出目录、自动重启、停止信号和基础加固项。
- 第二阶段后台异步导出清理定时器模板已补充：`deploy/systemd/scenic-ticket-admin-export-cleanup.service` 与 `deploy/systemd/scenic-ticket-admin-export-cleanup.timer` 可作为 Linux 部署的每日清理参考，实现由 `backend/tests/test_openapi_export_script.py` 覆盖 oneshot、非 root 用户、环境文件、受控导出目录、随机延迟、错过补跑和基础加固项。
- 第二阶段后台异步导出存储 provider 边界已补充：`ADMIN_EXPORT_STORAGE_PROVIDER` 当前只允许 `local`，未知或未实现 provider 会在配置校验阶段拒绝启动，API、worker 和清理脚本统一通过 storage factory 获取本地存储实现。
- 第二阶段后台异步导出队列 provider 边界已补充：`ADMIN_EXPORT_QUEUE_PROVIDER` 当前只允许 `database`，未知或未实现外部队列 provider 会在配置校验阶段拒绝启动，当前任务领取仍使用数据库行锁与状态条件。
- 第二阶段后台异步导出失败告警 provider 边界已补充：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-provider-boundary-design.md` 已明确 `ADMIN_EXPORT_ALERT_PROVIDER` 当前只允许 `disabled`，未知或未实现邮件、Slack、Webhook 告警 provider 会在配置校验阶段拒绝启动，避免部署误以为失败任务已能主动通知。
- 第二阶段后台异步导出最终失败告警事件记录已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-alert-event-design.md` 已明确 worker 最终失败只写内部 `admin_export_job_alert_event`，不发送真实通知也不新增前端页面；实现由 `backend/tests/test_admin_export_jobs_api.py` 与 `backend/tests/test_schema_contract.py` 覆盖 retryable `PENDING` 不记录、最终 `FAILED` 记录、事件字段脱敏、SQL 参数绑定、schema 和迁移。
- 第二阶段后台异步导出告警事件只读查询 API 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-list-design.md` 和 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-filter-design.md` 已明确管理员可通过 `GET /api/admin/export-job-alert-events` 分页查询内部告警事件，不发送真实通知也不新增前端页面；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖管理员权限、无 CSRF 只读 GET、`jobId/exportType/fileFormat/errorCode/acknowledged/closed/dateFrom/dateTo` 筛选、非法筛选拒绝、DTO 防泄露、OpenAPI 和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件汇总 API 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-summary-design.md` 已明确管理员可通过 `GET /api/admin/export-job-alert-events/summary` 查看内部告警事件总量、确认状态和 `byErrorCode` 聚合，不发送真实通知也不新增前端页面；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖管理员权限、无 CSRF 只读 GET、`exportType/fileFormat/closed/dateFrom/dateTo` 筛选、`closed/open` 计数、非法筛选拒绝、聚合 DTO 防泄露、OpenAPI 和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件确认 API 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-acknowledge-design.md` 已明确管理员可通过 `POST /api/admin/export-job-alert-events/{event_id}/acknowledge` 标记内部告警事件已处理，不发送真实通知也不新增前端页面；实现由 `backend/tests/test_admin_export_jobs_api.py`、`backend/tests/test_openapi_contract.py` 和 `backend/tests/test_schema_contract.py` 覆盖管理员权限、CSRF、第一次确认获胜、备注长度、DTO 防泄露、OpenAPI、schema 迁移和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件批量确认 API 已实现：规格 `docs/superpowers/specs/2026-07-03-admin-export-alert-event-batch-acknowledge-design.md` 已明确管理员可通过 `POST /api/admin/export-job-alert-events/batch-acknowledge` 批量确认内部告警事件；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖管理员权限、CSRF、第一次确认获胜、逐项不存在失败、备注长度、重复/空列表/snake_case/额外字段拒绝、DTO 防泄露和 OpenAPI。
- 第二阶段后台异步导出告警事件关闭和重开 API 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-close-reopen-design.md` 已明确管理员可通过 `POST /api/admin/export-job-alert-events/{event_id}/close` 和 `/reopen` 管理内部告警事件生命周期，不发送真实通知也不新增前端页面；实现由 `backend/tests/test_admin_export_jobs_api.py`、`backend/tests/test_openapi_contract.py` 和 `backend/tests/test_schema_contract.py` 覆盖管理员权限、CSRF、第一次关闭获胜、关闭备注长度、DTO 防泄露、OpenAPI、schema 迁移和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件批量关闭 API 已实现：规格 `docs/superpowers/specs/2026-07-03-admin-export-alert-event-batch-close-design.md` 已明确管理员可通过 `POST /api/admin/export-job-alert-events/batch-close` 批量关闭内部告警事件；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖管理员权限、CSRF、第一次关闭获胜、逐项不存在失败、备注长度、重复/空列表/snake_case/额外字段拒绝、DTO 防泄露和 OpenAPI。
- 第二阶段后台异步导出告警事件关闭筛选和汇总增强已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-closed-filter-summary-design.md` 已明确管理员可通过 `closed=true/false` 查询活跃或已关闭告警，并在汇总中查看 `closed/open` 顶层和错误码分组计数；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖非法关闭状态、DTO 防泄露和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件去重静默已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-dedupe-design.md` 已明确同一个 `job_id/error_code/alert_source` 的未关闭告警会折叠到原事件；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_schema_contract.py` 覆盖 `occurrenceCount/lastSeenAt`、关闭后重新创建、SQL 参数绑定、schema 和迁移。
- 第二阶段后台异步导出告警事件类型和格式筛选已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-type-format-filter-design.md` 已明确管理员可通过 `exportType/fileFormat` 筛选告警事件列表和汇总；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖大小写归一化、非法导出类型、非法文件格式、DTO 防泄露和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件删除 API 已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-alert-event-delete-design.md` 已明确管理员可通过 `DELETE /api/admin/export-job-alert-events/{event_id}` 删除已关闭告警事件；实现由 `backend/tests/test_admin_export_jobs_api.py`、`backend/tests/test_openapi_contract.py` 和 `backend/tests/test_security_basics.py` 覆盖管理员权限、CSRF、未关闭事件拒绝、事件不存在、CORS DELETE 预检、DTO 防泄露、OpenAPI 和 SQL 参数绑定。
- 第二阶段后台异步导出告警事件批量删除 API 已实现：规格 `docs/superpowers/specs/2026-07-03-admin-export-alert-event-batch-delete-design.md` 已明确管理员可通过 `POST /api/admin/export-job-alert-events/batch-delete` 批量删除已关闭告警事件；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖管理员权限、CSRF、逐项未关闭/不存在失败、重复/空列表/额外字段拒绝、DTO 防泄露、OpenAPI 和复用单删 SQL 参数绑定。
- 第二阶段后台异步导出 RUNNING 超时最终失败告警事件记录已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-running-timeout-alert-event-design.md` 已明确 RUNNING 超时耗尽重试额度落为 `FAILED + ADMIN_EXPORT_JOB_WORKER_TIMEOUT` 时写内部 `WORKER_FINAL_FAILURE` 事件；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖可重试回 `PENDING` 不记录、最终失败记录、事件字段脱敏和 SQL 参数绑定。
- 支付 provider 边界已补充：`PAYMENT_PROVIDER` 当前只允许 `mock`，未知真实支付 provider 会在配置校验阶段拒绝启动；实现由 `backend/tests/test_config_db.py` 覆盖默认值、大小写归一化和未知 provider 拒绝。
- 短信 provider 边界已补充：`SMS_PROVIDER` 当前只允许 `disabled`，未知真实短信 provider 会在配置校验阶段拒绝启动；实现由 `backend/tests/test_config_db.py` 覆盖默认值、大小写归一化和未知 provider 拒绝。
- 第二阶段后台异步导出失败任务重试已实现：`POST /api/admin/export-jobs/{job_id}/retry` 只允许管理员携带 CSRF 将 `FAILED` 任务重置为 `PENDING`，实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_contract.py` 覆盖重试成功、非失败状态拒绝、任务不存在、权限、CSRF、DTO 防泄露、OpenAPI 和 SQL 参数绑定。
- 第二阶段后台异步导出失败任务自动重试已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-auto-retry-design.md` 已明确只对 worker 未预期异常自动重试一次，业务/校验失败、unsupported 和手动中断不自动重试；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_schema_contract.py` 覆盖自动重试、耗尽失败、手动 retry 重置计数、schema 约束、迁移 SQL 和 SQL 参数绑定。
- 第二阶段后台异步导出自动重试延迟领取已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-retry-backoff-design.md` 已明确可重试任务使用内部 `next_attempt_at` 延迟 60 秒后再允许 worker 领取；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_schema_contract.py` 覆盖立即处理拿不到任务、延迟到期后再领取、手动 retry 清空延迟、成功/最终失败清空延迟和旧库迁移。
- 第二阶段后台异步导出 RUNNING 超时回收已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-running-timeout-design.md` 已明确 worker 每轮领取前回收超过 30 分钟的 `RUNNING` 任务；实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_schema_contract.py` 覆盖未耗尽重试额度回到 `PENDING` 并优先处理、耗尽后落为 `FAILED + ADMIN_EXPORT_JOB_WORKER_TIMEOUT`、`status + started_at` 索引迁移和 SQL 参数绑定。
- 第二阶段后台异步导出本地孤儿文件补偿清理已实现：规格 `docs/superpowers/specs/2026-07-02-admin-export-job-orphan-file-cleanup-design.md` 已明确 worker 写出文件后成功落库失败时会尽力删除刚生成的本地文件；实现由 `backend/tests/test_admin_export_jobs_api.py` 覆盖 mark success 返回 `None`、抛异常和正常成功路径。
- 第二阶段后台异步导出本地文件清理已实现：`scripts/cleanup-admin-export-files.py` 可按保留天数和批量上限清理过期 `SUCCEEDED` 导出文件，实现由 `backend/tests/test_admin_export_jobs_api.py` 和 `backend/tests/test_openapi_export_script.py` 覆盖受控存储目录、路径穿越拒绝、缺失文件元数据清理、非过期/失败任务不清理、SQL 参数绑定、脚本 JSON 输出和错误脱敏。
- 第二阶段模拟支付回调安全边界已实现：规格 `docs/superpowers/specs/2026-06-30-mock-payment-callback-design.md` 已明确 HMAC 验签、时间戳防重放、事件幂等、DTO 和验收边界；实现由 `backend/tests/test_mock_payment_callback_api.py` 覆盖签名、时间戳、重复事件、金额不匹配、订单状态、库存不足、SQL 参数绑定和错误脱敏。
- 第二阶段后台订单 CSV 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-order-export-design.md` 已明确文件响应、日期过滤、管理员权限、敏感字段边界和 CSV 公式注入防护；实现由 `backend/tests/test_admin_report_export_api.py` 覆盖 CSV 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、公式注入防护和 SQL 参数绑定。
- 第二阶段后台订单 XLSX 导出已实现：规格 `docs/superpowers/specs/2026-06-30-admin-order-xlsx-export-design.md` 已明确文件响应、日期过滤、管理员权限、敏感字段边界、公式注入防护、XML 1.0 非法控制字符清洗和无新增运行时依赖；实现由 `backend/tests/test_admin_report_export_api.py` 与 `backend/tests/test_openapi_contract.py` 覆盖 XLSX 下载、管理员权限、只读 GET 无 CSRF、日期范围错误、敏感字段防泄露、无公式节点、XML 1.0 非法控制字符清洗和 OpenAPI 文件响应。
- 支付流程覆盖幂等、库存条件更新、票码随机性、非业务异常脱敏、事务失败回滚和不继续出票/更新订单。
- 订单详情、支付和取消按当前 session 过滤归属；不存在或非当前游客订单统一返回 `ORDER_NOT_FOUND`，不暴露订单是否存在。
- 请求日志只记录 method、path、status、安全化 request id 和耗时，不记录 query、请求体、Cookie 或 CSRF。

## 安全证据

安全清单逐项映射见 `docs/backend-security-audit.md`。

当前自动化证据集中在：

- `backend/tests/test_auth_api.py`
- `backend/tests/test_catalog_api.py`
- `backend/tests/test_order_create_api.py`
- `backend/tests/test_payment_api.py`
- `backend/tests/test_my_orders_api.py`
- `backend/tests/test_security_basics.py`
- `backend/tests/test_error_contract.py`
- `backend/tests/test_success_contract.py`
- `backend/tests/test_openapi_contract.py`
- `backend/tests/test_api_dto_contract.py`
- `backend/tests/test_frontend_endpoint_contract.py`
- `backend/tests/test_visitor_flow_api.py`
- `backend/tests/test_admin_auth_api.py`
- `backend/tests/test_admin_orders_api.py`
- `backend/tests/test_admin_check_in_api.py`
- `backend/tests/test_admin_refund_api.py`
- `backend/tests/test_schema_contract.py`
- `backend/tests/test_admin_reports_api.py`
- `backend/tests/test_mock_payment_callback_api.py`
- `backend/tests/test_admin_report_export_api.py`

## 已知边界

- MVP 使用模拟支付，不接真实第三方支付。
- 登录限流使用进程内内存实现；多进程或多实例生产部署时应迁移到 Redis、网关或负载均衡层全局限流。
- 短信 provider 当前固定为 `SMS_PROVIDER=disabled`，真实短信验证码、短信签名、发送频率、验证码过期和供应商回调仍需单独设计。
- 退款通知、真实渠道退款流水号、真实渠道结算级财务对账、真实短信、真实支付渠道、真实告警和异步大文件下载仍属于后续第二阶段切片；当前完成管理员认证与权限基座、后台订单只读 API、单张票码核销 API、批量核销 API、撤销核销 API、批量撤销核销 API、撤销核销原因审计、核销审计日志、核销/撤销失败尝试审计、核销/撤销失败尝试审计 CSV 导出、核销审计日志检索、核销审计日志原因筛选、核销审计日志 CSV/XLSX 导出、全单模拟退款 API、部分模拟退款 API、退款审计日志、退款审计日志检索、退款审计日志 CSV/XLSX 导出、后台运营汇总报表 API、后台支付对账汇总 API、后台支付对账汇总 CSV/XLSX 导出、后台产品维度报表 API、后台产品维度报表 CSV/XLSX 导出、后台日报趋势报表 API、后台小时趋势报表 API、后台月度趋势报表 API、后台趋势报表补零、后台趋势报表 CSV/XLSX 导出、后台同步导出行数上限、后台异步导出任务基础、后台异步导出任务文件格式筛选、后台异步导出 filters 白名单、后台异步导出 worker 状态机基础、后台异步导出失败字段长度契约对齐、后台异步导出失败任务手动/自动重试、后台异步导出自动重试延迟领取、后台异步导出 RUNNING 超时回收、后台异步导出 RUNNING 超时最终失败告警事件记录、后台异步导出本地孤儿文件补偿清理、后台异步导出失败告警 provider 边界、后台异步导出最终失败告警事件记录、后台异步导出告警事件只读查询 API、后台异步导出告警事件汇总 API、后台异步导出告警事件确认 API、后台异步导出告警事件批量确认 API、后台异步导出告警事件关闭和重开 API、后台异步导出告警事件批量关闭 API、后台异步导出告警事件类型和格式筛选、后台异步导出告警事件删除 API、后台异步导出告警事件批量删除 API、后台异步订单明细 CSV/XLSX 生成 worker、后台异步核销审计 CSV/XLSX 生成 worker、后台异步核销失败审计 CSV/XLSX 生成 worker、后台异步退款审计 CSV/XLSX 生成 worker、后台异步支付对账 CSV/XLSX 生成 worker、后台异步产品维度报表 CSV/XLSX 生成 worker、后台异步日报趋势 CSV/XLSX 生成 worker、后台异步小时趋势 CSV/XLSX 生成 worker、后台异步月度趋势 CSV/XLSX 生成 worker、模拟支付回调安全边界，以及后台订单 CSV/XLSX 导出。
- 浏览器 E2E smoke 不是默认门禁，需要 `VERIFY_INTEGRATION_E2E=1` 显式开启。
- 前端视觉、响应式和页面交互验收由前端对话负责；本报告只确认后端和共享 API 互通门禁。
